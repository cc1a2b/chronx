"""chronx — a time-travel debugger for shell sessions.

Records the causal link between the commands you run and the filesystem
changes they produce, so any command's effects can be inspected, diffed,
blamed, and reverted after the fact.
"""

from __future__ import annotations

__version__ = "0.1.0"
